import abc
from shufflify import PluginBase

class shuffler(PluginBase):

    description = "Uses lowest-common-multiple to create even distributions (coming soon lol)"
    
    def shuffle(self,artists):
        return
